# 一致性分析：Memory Assets

## 结论

- 新主规格已把 `memory_assets` 提升为除 `learning` 外的统一真相源
- `MEA` 事件记忆、PM 正式策略资产、日摘要和投影层边界已清楚分离
- 协作痕迹与正式资产的界线已明确

## 仍需后续处理

- 代码层还没有正式落地 `MacroEventRecord`、`MacroDailyMemory`、`MemoryProjection`
- 更多运行态真实资产仍待逐模块补齐
