# 检查清单：系统模块化蓝图质量门禁

**用途**：验证本次蓝图文档是否已经覆盖当前系统事实、目标模块化边界、接口契约与迁移设计。  
**创建日期**：2026-03-11  
**关联功能**：[`spec.md`](specs/001-system-blueprint/spec.md)

## 内容质量

- [ ] CHK001 文档是否明确区分“当前系统事实”和“目标系统设计”，避免两者混写。
- [ ] CHK002 文档是否默认使用中文，并只在必要处保留英文标识符或协议字段。
- [ ] CHK003 文档是否避免空泛口号，所有模块、状态、接口都具备可追溯命名。
- [ ] CHK004 文档是否没有把实现细节错误包装成业务需求。

## 一致性与覆盖

- [ ] CHK005 是否完整列出 10 个顶层模块，并说明每个模块的职责与非职责。
- [ ] CHK006 是否完整列出 3 条跨域平面，并说明它们如何服务所有模块。
- [ ] CHK007 是否完整列出 4 个 Agent，并说明其输入、输出、自主性和升级规则。
- [ ] CHK008 是否给出 LLM 信息源分类，而不是只给 prompt 或文件名列表。
- [ ] CHK009 是否定义统一状态机，并覆盖策略、执行判断、执行、通知、失败降级与回放。
- [ ] CHK010 是否定义统一外部主动触发入口，并明确归属于状态机与编排器模块。
- [ ] CHK011 是否定义 OpenClaw 集成为独立适配层，而不是散落在多个模块内部。
- [ ] CHK012 是否定义结构化事件协议，足以支撑实时动画和历史回放。
- [ ] CHK013 是否定义事件类型命名、交付路径与 `memory_assets`/进程内总线关系，而不是只写“会用消息队列”。
- [ ] CHK014 是否说明哪些参数可以手动调整、如何审计、如何回滚。
- [ ] CHK015 是否补齐当前 live 运行态复现手册，并明确哪些事实在本地运行目录、OpenClaw 工作区、模型 artifact 和 LaunchAgents 中。
- [ ] CHK016 是否补齐量化小模型训练 / 推理 / artifact / 当前阈值与职责分工的复现说明。

## 交付准备度

- [ ] CHK017 当前代码和未来模块之间是否已经建立文件级映射。
- [ ] CHK018 是否给出渐进式迁移波次，而不是一次性大重写。
- [ ] CHK019 是否保留当前 live 运行态兼容假设，避免蓝图脱离真实部署。
- [ ] CHK020 后续任何实现任务是否都能回链到本蓝图中的某个模块、契约、状态或任务项。
